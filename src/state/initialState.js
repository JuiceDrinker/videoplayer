const initialState = {
  currentVideoSrc: "",
  videoWidth: "50%", //Could consider putting this as a const in top-level, and passing it down to the components that need it
};

export default initialState;
